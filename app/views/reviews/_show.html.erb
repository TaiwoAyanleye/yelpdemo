<table class="table">
	<tbody>
		<% @reviews.each do |review| %>
		<tr>
			<td>
				<div class="star-rating" data-score= <%= review.rating %> ></div>
				<p><% h(review.comment).gsub(/\n/, '<br/>').html.safe %></p>
			</td>
		</tr>
		<% end %>
	</tbody>
</table>